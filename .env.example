# Optional override; defaults to the verified legacy backend used by the current production flow.
VITE_API_BASE_URL=https://ai-planner-backend-i37s.onrender.com
# Optional override; defaults to the legacy JSON generation route.
VITE_GENERATE_PATH=/api/generate-itinerary
VITE_GOOGLE_MAPS_API_KEY=
